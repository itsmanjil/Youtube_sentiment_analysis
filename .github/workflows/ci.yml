name: CI

on:
  pull_request:
  push:

jobs:
  lint-python:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: backend/requirements-dev.txt
      - name: Install lint/audit tooling
        run: python -m pip install --upgrade pip && pip install -r requirements-dev.txt
      - name: Ruff (unused imports/vars, undefined names)
        run: ruff check .
      # requirements-dl.txt (torch/transformers) is included here rather than
      # given its own job: pip-audit resolves requirement files against PyPI
      # metadata without installing them, so this costs nothing extra to run
      # and doesn't need a separate runner. Before this, the exact packages
      # that carried a documented CVE fix (torch<2.6.0 / CVE-2025-32434, see
      # requirements-dl.txt's torch pin comment) had zero ongoing CI
      # vulnerability coverage -- a future downgrade or CVE regression there
      # would have gone undetected.
      - name: pip-audit (requirements.txt, requirements-dl.txt)
        run: pip-audit -r requirements.txt -r requirements-dl.txt

  backend:
    runs-on: ubuntu-latest
    env:
      DJANGO_ENV: test
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: backend/requirements.txt
      - name: Install backend dependencies
        run: python -m pip install --upgrade pip && pip install -r requirements.txt
      - name: Django system checks
        run: python manage.py check
      - name: Backend test suite
        run: python manage.py test app app_api users

  research-smoke:
    # Classical-model research/ci scripts are never exercised by `manage.py
    # test` (that suite mocks the sentiment engines) and have no other CI
    # coverage, so a refactor of src/sentiment or src/preprocessing can
    # silently break them. Run the lightweight ones end-to-end (small sample
    # sizes) against the models/data already committed in the repo, so a
    # broken import or signature change fails the build instead of only
    # surfacing the next time someone runs the script by hand.
    runs-on: ubuntu-latest
    env:
      DJANGO_ENV: test
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: backend/requirements.txt
      - name: Install backend dependencies
        run: python -m pip install --upgrade pip && pip install -r requirements.txt
      - name: Split leakage check
        run: >
          python scripts/prepare/check_split_leakage.py
          --train data/train.csv --val data/val.csv --test data/test.csv
      - name: Temperature scaling smoke run (classical + ensemble + meta-learner)
        run: >
          python research/ci/temperature_scaling.py
          --sample_val 200 --sample_test 200
          --output /tmp/research-smoke/

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install frontend dependencies
        run: npm ci
      - name: Lint (eslint)
        run: npm run lint
      - name: Dependency audit
        run: npm audit --omit=dev --audit-level=high
      - name: Frontend test suite
        run: npm test -- --run
      - name: Production build
        run: npm run build
