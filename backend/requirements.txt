# Research-only dependencies (Python 3.11+). Deep-learning extras
# (Hybrid-DL / Transformers) are in backend/requirements-dl.txt.
# pandas/numpy are pinned to versions that satisfy BOTH files so a single venv
# can install requirements.txt followed by requirements-dl.txt (or vice versa).

# nltk is pinned to a known-CVE fix floor (pip-audit, 2026-07-11).
nltk==3.9.4
numpy==1.26.4
# Matches the environment that actually produced the pinned model artifacts
# (models/*/*_metadata.json records pandas_version 3.0.0). Requires
# numpy>=1.26.0 on Python <3.14 (this project targets 3.11+), which
# numpy==1.26.4 above satisfies without a numpy bump.
pandas==3.0.0
scikit-learn==1.8.0
emoji==2.15.0
langdetect==1.0.9
wordcloud==1.9.6
matplotlib==3.10.8

# Notebooks (research narrative lives in notebooks/)
jupyterlab==4.6.1
ipykernel==7.1.0
nbconvert==7.17.1
