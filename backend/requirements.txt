# Pinned to backend/Pipfile.lock (Python 3.11+). Regenerate with `pipenv requirements`
# (note: Pipfile.lock must be regenerated separately after changing pins here).
# Deep-learning extras (Hybrid-DL / Transformers) are in backend/requirements-dl.txt.
# pandas/numpy are pinned to versions that satisfy BOTH files so a single venv can
# install requirements.txt followed by requirements-dl.txt (or vice versa) without
# the two-venv workaround previously documented in requirements-dl.txt.

# django/nltk/yt-dlp/python-dotenv are pinned to known-CVE fix floors
# (pip-audit, 2026-07-11); django stays on the 5.2.x LTS line rather than
# jumping to 6.0.
django==5.2.16
djangorestframework==3.16.1
django-cors-headers==4.9.0
djangorestframework-simplejwt==5.5.1
psycopg2-binary==2.9.11
# Background job queue for youtube/analyze/ (see app/views.py). ORM broker —
# no Redis/RabbitMQ needed, tasks are queued in the same database. Requires a
# separate `python manage.py qcluster` worker process; see README.md.
django-q2==1.10.0
django-picklefield==3.4.0
nltk==3.9.4
# numpy 2.4.6 is the latest 2.x release still supporting Python 3.11 (2.5.0+
# requires 3.12). Verified (2026-07-18) that the existing pickled classical
# artifacts (models/logreg, models/svm, models/tfidf — originally fit under
# numpy 1.26.4 / scikit-learn 1.8.0) load under this numpy+sklearn pair and
# produce byte-identical predictions (8 decimal places, 8 test sentences),
# and the full test suite passes unchanged. No retraining was needed or
# done — see the artifact-verification comments in
# src/sentiment/engines/*_engine.py's _validate_fitted for the safety net if
# a future sklearn release ever does break pickle compatibility.
numpy==2.4.6
# models/*/*_metadata.json records pandas_version 3.0.0 as what actually
# produced the pinned model artifacts; bumped here to 3.0.3 (same major
# version, verified compatible — see the numpy comment above) rather than
# left unbumped, since requirements-dl.txt's numpy pin must match this file
# exactly for the documented single-venv install workflow.
pandas==3.0.3
python-dotenv==1.2.2
scikit-learn==1.9.0
google-api-python-client==2.188.0
google-auth-httplib2==0.3.0
google-auth-oauthlib==1.2.4
youtube-comment-downloader==0.1.78
yt-dlp==2026.6.9
emoji==2.15.0
# Replaced langdetect (2010-era, unreliable on short YouTube comment text —
# see app/youtube_preprocessor.py). 2.1.1 is the latest release still
# supporting Python 3.11 (2.2.0+ requires 3.12).
lingua-language-detector==2.1.1
wordcloud==1.9.6
matplotlib==3.10.8
